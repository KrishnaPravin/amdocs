package com.amdocs.CleanCodePractices.single_entry_single_exit;

public class SingleEntrySingleExit {
	
	final String FAIL = "Fail";
	

	public static void main(String[] args) {
		
	}
	
	String getExamResult(int examMark) {
		String result;
		if (examMark < 50) {
			result =  FAIL;
		}
		return result;
	}
	
	
	String getExamReasult(int mark) {
		String result;
		if (isNumeric(s)) {
			if (r > 0) {
				ret = Long.parseLong(s,r);
			} else {
				ret = ERR_BAD_RADIX;
			}
		} else {
			  ret = ERR_PARSE;
		  }
		  return ret;
	}
	
	boolean isNumeric(String stringToCheck) {
		return true;
	}


}
